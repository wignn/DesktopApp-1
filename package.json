{
  "name": "desktop-1",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start:api": "cd api && nodemon",
    "start:app": "cd app && npm run dev",
    "start:all": "npm-run-all --parallel start:api start:app",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "wignn",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "electron-store": "^10.0.0"
  }
}
